alignlen = 64
#arch = mic
#arch = hetero

CC  = /opt/intel/bin/icc
CXX = /opt/intel/bin/icpc
FC  = /opt/intel/bin/ifort
AR  = /opt/intel/bin/xiar rcs

ifeq "${arch}" "mic"
OPTFLAGS = -mmic
else ifeq "${arch}" "avx"
OPTFLAGS = -m64 -xAVX
else
OPTFLAGS = -m64 -xHost
endif
